# --- API keys (server-side only; never expose to the client) ---

# Primary model chain. Free key, no card required: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Second tier (Groq, gpt-oss-120b). Free key, no card: https://console.groq.com/keys
GROQ_API_KEY=

# Optional third tier (OpenRouter :free models). Unfunded = 50 req/day;
# a one-time $10 credit purchase lifts free-model usage to 1,000 req/day.
# https://openrouter.ai/keys
OPENROUTER_API_KEY=

# --- Server ---

PORT=8787

# Comma-separated list of origins allowed to call the API.
# Dev default below; set to your production origin when deploying.
ALLOWED_ORIGIN=http://localhost:5173

# Set to 1 only when running behind a trusted reverse proxy (Railway, Render, nginx…)
# so per-IP rate limits see the real client address.
TRUST_PROXY=0
