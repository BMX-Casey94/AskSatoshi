# Windows cannot create the workspace symlinks without Developer Mode/admin rights.
# install-links installs workspace packages as real directories instead — identical
# dependency hoisting, portable across Windows dev machines and Vercel's Linux builders.
install-links=true
